import { UPDATE_ACCOUNT_ERROR, UPDATE_ACCOUNT_PENDING, UPDATE_ACCOUNT_SUCCESS } from '../constants';

const actions = {
    update: {
        pending: UPDATE_ACCOUNT_PENDING,
        success: UPDATE_ACCOUNT_SUCCESS,
        error: UPDATE_ACCOUNT_ERROR
    }
};

export function updateAccount(user) {

}